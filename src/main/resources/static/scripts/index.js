// Set root element
const root = ReactDOM.createRoot(document.getElementById('root'));

// Set up page
const App = () => {
  return (
    <div>
      <h1>Hello World</h1>
    </div>
  );
};

// Render contents
root.render(<App />);
